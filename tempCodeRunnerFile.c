    // // Convert to uppercase using strupr() (note: non-standard in some compilers)
    // for (int i = 0; greeting[i]; i++) {
    //     greeting[i] = toupper(greeting[i]);
    // }
    // printf("Uppercase String: %s\n", greeting);